# Project patches

`skia/` holds the two AROS CPU patches for the pin in `upstreams.json`.
The series is required; an empty series would mean an unpatched pin and
would not build on ABIv11.

Commands: [the patching guide](../docs/patching.md).
